{
  "packages": {
    ".": {
      "release-type": "rust",
      "package-name": "netbeat"
    }
  },
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "pull-request-title-pattern": "release(${component}): v${version}"
}
